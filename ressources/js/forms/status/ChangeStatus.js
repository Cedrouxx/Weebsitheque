export default class ChangeStatus{

    constructor(){
        
        document.querySelectorAll('#changeStatusButton').forEach(e => e.remove());

        document.querySelectorAll('#changeStatusSelect')
    }

}